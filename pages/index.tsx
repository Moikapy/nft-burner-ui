import type {NextPage} from 'next';

const Home: NextPage = () => {
  return (
    <>
      
      Any user can connect its wallet and select a burner they want to play
      with. This page is a grid of all the burners registered on the app (with a
      default image for now)
    </>
  );
};

export default Home;
